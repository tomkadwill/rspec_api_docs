require 'spec_helper'

describe RspecApiDocs do
  it 'has a version number' do
    expect(RspecApiDocs::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
